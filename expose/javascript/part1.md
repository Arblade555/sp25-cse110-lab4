1. This would print "values added: " and the number (num1 + num2) because var tolerates redeclarations and re-assignments
2. The code would return an error that the var result is not defined. This is because var ignores if 
statements and is equivalent to it being declared at the very front.
3. Since it is not enclosed within if statement or in loops, it may cause unexpected issues relating to undefined variables  
4. This would print out "values added: " and the number (num1 + num2) because let can be re assigned a different value later down the line
5. This would return an error as the variable result is not defined in that scope because let doesn't definned outside of the if statement
6. Line 9 would return 0 because when using the const variable it cannot be reassigned after the first assignment
7. It would return an error that result has not been defined in that scope